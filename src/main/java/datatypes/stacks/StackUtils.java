package datatypes.stacks;

/**
 * Created by Alexander on 02.06.2017.
 */
public class StackUtils {


    /** 3.2
     Write a program to sort a stack such that the smallest items are on the top. You can use
     an additional temporary stack, but you may not copy the elements into any other data structure
     (such as an array). The stack supports the following operations: push, pop, peek, and is Empty.
     * */
//    public static  StackCustom<Integer> sort(StackCustom<Integer> stack){
//        StackCustom<Integer> tempStack = new StackCustom<Integer>();
//
//        Integer temp=stack.pop();
//        while(!stack.isEmpty()){
//            if(stack.peek()>temp) tempStack.push(stack.pop());
//            if(stack.peek()<temp) tempStack.push(stack.pop());
//        }
//    }
}
